export const Student = ({ name, noSeat }) => {
    return (
      <div>
        <h3>Student {name}</h3>
        <p>No seat {noSeat}</p>
      </div>
    );
  };
  
  export default Student;  